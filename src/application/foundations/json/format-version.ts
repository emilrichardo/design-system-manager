// T032 (004) - Version independiente del contrato JSON foundations. No reutiliza ni modifica
// JSON_FORMAT_VERSION de 003.

/** Version literal estable del formato JSON foundations v1. */
export const FOUNDATIONS_JSON_FORMAT_VERSION = "1.0.0";

/** Tipo literal de la version del formato JSON foundations. */
export type FoundationsJsonFormatVersion = typeof FOUNDATIONS_JSON_FORMAT_VERSION;
