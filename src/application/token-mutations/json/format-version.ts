// T035 (008) — Format version propio del contrato JSON de mutaciones; independiente de `003`/`004`/
// `006`/`007` (no comparte tipo ni union con esos contratos).
export const TOKEN_MUTATION_JSON_FORMAT_VERSION = "1.0.0";

export type TokenMutationJsonFormatVersion = typeof TOKEN_MUTATION_JSON_FORMAT_VERSION;
